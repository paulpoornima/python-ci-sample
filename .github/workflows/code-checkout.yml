name: Code Checkout Workflow

on:
  push:
    branches: [main]
  pull_request:

jobs:
  checkout-code:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v3
